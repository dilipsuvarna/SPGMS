export default function AboutPage() {
  return (
    <main className="page">
      <section className="card">
        <h2>About SPGMS</h2>
        <p>SPGMS helps citizens report civic problems to the right department with minimal friction. The system uses AI for classification, priority, and duplicate detection. Complaints are assigned to officers automatically and tracked until closure.</p>
      </section>
    </main>
  );
}

